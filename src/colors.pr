{*

Generate all color tokens as named variables.
Tokens are named by their group path and then name, 
and their value rendered using `rendered-color` blueprint

*}
{[ let brand = ds.currentBrand() /]}
{[ const allTokenGroups = ds.tokenGroupsOfType("Color", brand.id) /]}
{[ for group in allTokenGroups ]}

{[ const sortedTokens = sortTokensByName(ds.tokensByGroupId(group.id)) /]}
{[ for token in sortedTokens ]}
{[ inject "rendered-token-var" context token /]}


{[/]}
{[/]}



